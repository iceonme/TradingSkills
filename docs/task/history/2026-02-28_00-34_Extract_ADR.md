# 提取与完善 CTS1 核心能力剥离架构指南 (内部接口协议)

> **日期**: 2026-02-28 01:19
> **描述**: 完善架构解耦设计，确立内部“策略接口协议”以及分层的标准化约束机制。

## 变更内容
- 已阅读项目最新的 `implementation_plan.md.resolved` 及用户最新的架构反馈。
- 修改并升级了 `c:\Projects\ta\TradingSkills\docs\architecture\ADR-001-System_Decoupling.md`：
  - 将原本具有侵略性的 TAS-20 命名降级为务实的 **TS 策略驱动型接口标准 (Interface Protocol)**。
  - 规定所有的策略 Skill 必须通过依赖注入（数据接收器+操作手柄）获取数据和发信号。
  - **特别补充项**：基于策略数据源千奇百怪的现实，**调宽了 DataFeed 的限制**，它只被视作宽泛的接收接口流，由宿主按策略期望送入数据即可，不做强制全局大一统；但**强化了执行指令的标准化输出**，确保 PA 和执行器能无障碍对接。
  - 更新了对应的 Mermaid 架构图，改为更通用的文本表述和接收口定义。

## 验证与测试
- 路径: 文件成功更新至 `c:\Projects\ta\TradingSkills\docs\architecture\ADR-001-System_Decoupling.md`。
- 架构逻辑形成完整闭环（宿主环境 -> 数据适配器清洗 -> 标准格式传入 TAS-20 策略引擎 -> 触发动作发给标准 Executor）。
